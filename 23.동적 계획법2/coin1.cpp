#include <iostream>
#include <vector>

int main() {
    int n, k;
    std::cin >> n >> k;
    std::vector<int> value(n, 0);

    for(int i = 0 ; i < n ; i++) {
        std::cin >> value[i];
    }

    
}